Equinet A2: A1 stages Companies/initial People in Twenty. A2 runs bounded official-site Firecrawl research before FullEnrich, preserves Company contacts/socials, retains at most two website People regardless of role. Generic Company contacts never populate People; personal webmail beside staff is review-only. HubSpot/outreach disabled. Step 11 LLM artifacts (live-validated): person-proposals.json = run_id/company_id matching packet + reviewed_block_ids covering ALL person_evidence_blocks once + proposals with unique proposal_id, block_id, person_name, source_mention/company_relationship_evidence/role_text as exact substrings of the cited block; only relationship 'current' accepted; role-only blocks cannot support exact names. website-decisions.json people rows echo observed_name/observed_role EXACTLY plus role_priority/identity_status/company_match_status/role_status; retained = VERIFIED+CONFIRMED+CURRENT_AT_COMPANY. Noise: CDN timestamps leak as phones; shared_surname_split mints false people from prose; misspelling duplicates across pages.
§
Equinet A2 batch enrichment is chat-triggered with the LLM as decision-maker over staged connector scripts; artifacts persist outside chat for idempotent resume. After a Contact Enrichment timeout, check the run dir for contact-results.json, then re-run the same script — persisted idempotency keys reuse the in-flight provider batch without double-charging.
§
Twenty A2 metadata uses `a2RoleStatus`: `CURRENT_AT_COMPANY`, `NOT_CURRENT_AT_COMPANY`, `UNVERIFIED`. Provider ID field `a2FullenrichPersonid` is unique. Company/Person writes, associations and status filtering are API-capable; no Twenty webhooks are required.
§
Equinet A2 controlled-pilot FullEnrich credit limits are 50 credits per run, 100 per UTC day, and 250 total. The user explicitly decided that no spend approver or consumption owner is required for these caps.
§
Equinet A2 People Search order is primary roles → exact generic Owner → verified current linked secondary reuse → paid secondary roles → no target, with validation between stages. Technical failures do not trigger fallback.